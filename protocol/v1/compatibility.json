[
  {
    "id": "current",
    "installedSchemaVersion": 18,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": true
  },
  {
    "id": "schema-newer-inside-major-line",
    "installedSchemaVersion": 19,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": true,
    "note": "Inside a major line a migration only adds, so a newer schema still serves this client. Refusing it would make every rolling deployment an outage."
  },
  {
    "id": "served-protocol-undeclared",
    "installedSchemaVersion": 18,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [],
    "compatible": true,
    "note": "A schema that declares no served protocol has made no statement to enforce."
  },
  {
    "id": "schema-not-installed",
    "installedSchemaVersion": null,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [],
    "compatible": false,
    "refusalCode": "schema-not-installed",
    "refusal": "Refuse every mutation because workhorse.schema_version cannot be read."
  },
  {
    "id": "schema-too-old",
    "installedSchemaVersion": 16,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": false,
    "refusalCode": "schema-too-old",
    "refusal": "Refuse every mutation until the database is migrated: the release calls functions a schema below its minimum does not carry."
  },
  {
    "id": "schema-below-the-dashboard-reads",
    "installedSchemaVersion": 5,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": false,
    "refusalCode": "schema-too-old",
    "refusal": "Refuse every mutation because the release's dashboard reads functions schema 5 does not carry.",
    "note": "Schema 4 and schema 5 declare protocol 4 and pass every protocol test, so only the schema floor refuses them. Without it the process starts and fails later with SQLSTATE 42883."
  },
  {
    "id": "schema-below-the-statement-catalogues",
    "installedSchemaVersion": 4,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": false,
    "refusalCode": "schema-too-old",
    "refusal": "Refuse every mutation because the release's statement catalogues call functions schema 4 does not carry."
  },
  {
    "id": "schema-no-longer-serves-client",
    "installedSchemaVersion": 18,
    "clientProtocolVersion": 4,
    "servedProtocolVersions": [5],
    "compatible": false,
    "refusalCode": "schema-too-new",
    "refusal": "Refuse every mutation because a major release stopped serving this client's protocol."
  },
  {
    "id": "client-protocol-too-old",
    "installedSchemaVersion": 18,
    "clientProtocolVersion": 0,
    "servedProtocolVersions": [1],
    "compatible": false,
    "refusalCode": "client-protocol-too-old",
    "refusal": "Refuse every mutation because the client protocol is below the supported range."
  },
  {
    "id": "client-protocol-too-new",
    "installedSchemaVersion": 18,
    "clientProtocolVersion": 5,
    "servedProtocolVersions": [1, 2, 3, 4],
    "compatible": false,
    "refusalCode": "client-protocol-too-new",
    "refusal": "Refuse every mutation because the client protocol is outside the supported range."
  }
]
