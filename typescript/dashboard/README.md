# @workhorse/dashboard

Compatibility package for the shared application in `dashboard/app` and the TypeScript backend in
`typescript/dashboard-server`. Existing imports keep their public names after the repository split.
