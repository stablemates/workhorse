# `@workhorse/dashboard-contract`

This type-only package defines the standalone entry point shared by `@workhorse/core` and
`@workhorse/dashboard`. Most applications should install `@workhorse/dashboard` and use its public
server APIs instead of importing this package directly.
