# `@workhorse-js/dashboard-contract`

This type-only package defines the standalone entry point shared by `@workhorse-js/core` and
`@workhorse-js/dashboard`. Most applications should install `@workhorse-js/dashboard` and use its public
server APIs instead of importing this package directly.
