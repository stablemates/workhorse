# Workhorse for Go

The Go module will use `github.com/stablemates/workhorse/go` and `go/vX.Y.Z` release tags. Its
transactional enqueue client is reserved for [WOR-78](https://linear.app/workhorse-run/issue/WOR-78/p2-14a-publish-the-go-transactional-enqueue-client).
