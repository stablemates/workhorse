{
  "acceptances": [
    {
      "advisory": 1130709,
      "githubAdvisoryId": "GHSA-fxqj-rqcc-2cmp",
      "module": "postcss",
      "workspacePackages": ["site"],
      "reason": "Outside the published closure. No published package depends on postcss; it is reached only through the private `site` package's build-time Vite. The advisory reads a `.map` file named by a `sourceMappingURL` comment in the stylesheet being compiled, and `site` compiles only stylesheets this repository writes.",
      "reviewBy": "2027-03-01"
    },
    {
      "advisory": 1139427,
      "githubAdvisoryId": "GHSA-2v37-7h3g-55p8",
      "module": "nanoid",
      "workspacePackages": ["site"],
      "reason": "Outside the published closure. No published package depends on nanoid; it is reached only through the private `site` package's build-time Vite, under postcss. The loop needs a caller asking a custom generator for a zero-length identifier, and postcss asks for a fixed non-zero length.",
      "reviewBy": "2027-03-01"
    }
  ]
}
